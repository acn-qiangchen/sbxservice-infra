---
description: 
globs: 
alwaysApply: true
---

# Your rule content

- You can @ files here
- You can use markdown but dont have to
- [CONVENTIONS.md](mdc:docs/CONVENTIONS.md)
- alwasy check the right directory before executing command
- when executing aws cli, always check if AWS_PROFILE environment variable exists. if AWS_PROFILE does not exist, note the user to set it.
- when architecture is changed. alwasy remember to update architecture blueprint documents under docs/architecture directory.
- when build docker image, alwasy keep in mind set the `platform` flag to proper value.

